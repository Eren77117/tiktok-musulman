{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '=== DIAG ===' && ls dist/ && echo '=== ENV ===' && echo NODE_ENV=$NODE_ENV PORT=$PORT && npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
