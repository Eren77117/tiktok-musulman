{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo FILES: && ls && echo DIST: && ls dist/ 2>&1 && npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
